# node_module_boilerplate
simple node module boilerplate

## Usage
- glit clone this repo 
- `npm init` to add details. and `package.json` 
  - set test to `jasmine`   
- `jasmine init`to add spec folder.
  - add `index_spec.js` inside spec folder   
